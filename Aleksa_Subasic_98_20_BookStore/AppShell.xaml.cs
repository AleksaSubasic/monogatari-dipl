using Aleksa_Subasic_98_20_BookStore.Views;

namespace Aleksa_Subasic_98_20_BookStore
{
    public partial class AppShell : Shell
    {
        public AppShell()
        {
            InitializeComponent();
            // Routing.RegisterRoute(nameof(LoginPage), typeof(LoginPage));
            // Routing.RegisterRoute(nameof(RegisterPage_1), typeof(RegisterPage_1));
            // Routing.RegisterRoute(nameof(RegisterPage_2), typeof(RegisterPage_2));
            // Routing.RegisterRoute(nameof(RegisterPage_3), typeof(RegisterPage_3));
            // Routing.RegisterRoute(nameof(SuccReg), typeof(SuccReg));
            // Routing.RegisterRoute(nameof(MainPage), typeof(MainPage));
            // Routing.RegisterRoute(nameof(ExplorePage), typeof(ExplorePage));
            // Routing.RegisterRoute(nameof(CartPage), typeof(CartPage));
            // Routing.RegisterRoute(nameof(FavoritesPage), typeof(FavoritesPage));
            // Routing.RegisterRoute(nameof(ProfilePage), typeof(ProfilePage));
            // Routing.RegisterRoute(nameof(BookDetailPage), typeof(BookDetailPage));
        }
    }
}
